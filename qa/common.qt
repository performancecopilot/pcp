. /etc/pcp.conf
. ./common.sh
. ./common.check
. ./common.filter
. ./common.config

tmp=/tmp/$$
here=`pwd`
sudo=sudo
host=`hostname`

if [ -f /usr/lib/libpcp_qmc.so ]
then
    :
else
    _notrun "libpcp_qmc not installed"
fi
