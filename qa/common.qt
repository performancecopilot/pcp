. /etc/pcp.conf
. ./common.sh
. ./common.check
. ./common.filter
. ./common.config

tmp=/tmp/$$
here=`pwd`
sudo=sudo
host=`hostname`

# hackery for dbus-based environments
#
if which dbus-launch >/dev/null 2>&1
then
    eval `dbus-launch | sed -n -e '/BUS_ADDRESS=/{
s/^/export /
p
}'`
fi
