#pmlogconf-setup 2.0
ident	XFS data and log I/O traffic [Linux]
probe	xfs.log.writes exists ? include : exclude
	xfs.log.writes
	xfs.log.blocks
	xfs.log.noiclogs
	xfs.read
	xfs.write
	xfs.read_bytes
	xfs.write_bytes
	xfs.buffer
	xfs.quota.cachehits
