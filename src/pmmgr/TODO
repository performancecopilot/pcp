- signal handling
	- execve
	- if (quit) return
- pmlogrewrite only if merge
- tie-break connection-string for multiply-reachable pmcd via other criteria? "shortest url"?
- optionally delay pm*conf
- pmlogreduce
- log aging in background while new pmlogger's already running
- old log compression (until we get libpcp zlib or something)
- email error reporting?
- qa
- port to mingw?
- port to cygwin?
- pmlogger/pmie .log rotation
- backoff for failing daemon
