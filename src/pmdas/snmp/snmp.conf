# Simple, first pass at a config file.  I expect to need a more expressive
# file format in the future

# hashes begin comments
# comments can begin in the # middle of a line

# define a host
#host $hostname $community
# currently only community based auth is supported
host localhost public

# load a mib (future) - gives name to number oid mappings
#mib $mibfilename

# static mapping
# id's start at 1 and redefininitions result in the last define winning
# the snmptype is used to calculate the pcp metric type to use
# the text is used as the metric short help text
#map $oid $snmptype $id $text

map 1.3.6.1.2.1.1.3.0 TIMETICKS 1 sysUpTime
map 1.3.6.1.2.1.1.5.0 STRING	2 sysName

map 1.3.6.1.2.1.2.2.1.2.1  STRING    10 ifDescr.1
map 1.3.6.1.2.1.2.2.1.2.2  STRING    + ifDescr.2
map 1.3.6.1.2.1.2.2.1.2.3  STRING    + ifDescr.3
map 1.3.6.1.2.1.2.2.1.5.1  GAUGE32   + ifSpeed.1
map 1.3.6.1.2.1.2.2.1.5.2  GAUGE32   + ifSpeed.2
map 1.3.6.1.2.1.2.2.1.5.3  GAUGE32   + ifSpeed.3
map 1.3.6.1.2.1.2.2.1.10.1 COUNTER32 + ifInOctets.1
map 1.3.6.1.2.1.2.2.1.10.2 COUNTER32 + ifInOctets.2
map 1.3.6.1.2.1.2.2.1.10.3 COUNTER32 + ifInOctets.3

# TODO - work out some kind of static walk define
#map tree $oid $id_start $id_max $text_prefix

# define the high water mark
#maxstatic $pcpval

# set an option
#set key val
set debug 1

