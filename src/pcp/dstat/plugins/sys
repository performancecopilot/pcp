#
# pcp-dstat(1) configuration file - see pcp-dstat(5)
#

[sys]
width = 5
label = system
type = raw
printtype = decimal
interrupts = delta(kernel.all.intr)
interrupts.label = int
context_switches = delta(kernel.all.pswitch)
context_switches.label = csw
