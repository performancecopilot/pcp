provider probe {
	probe PMCD (int, int, int, int);
/*
	probe ADD_AGENT (int, int, int);
	probe ADD_CLIENT (int, int, int);
	probe DEL_AGENT (int, int, int);
	probe DEL_CLIENT (int, int, int);
	probe AT_EOF (int, int, int);	
	probe RECV_ERR (int, int, int);
	probe RECV_PDU (int, int, int);
	probe RECV_TIMEOUT (int, int, int);
	probe WRONG_PDU (int, int, int);
	probe XMIT_ERR (int, int, int);
	probe XMIT_PDU (int, int, int, int, int);
*/
}
