# migrate and clean configs

#DEBUG:
#echo >>$PCP_LOG_DIR/install.log
#date >>$PCP_LOG_DIR/install.log

for base in pmcd pmie pmlogger pmproxy pmwebd
do
    _clean_configs -v $PCP_SYSCONF_DIR/$base /var/lib/pcp/config/$base /etc/$base /etc/pcp/$base /etc/sysconfig/$base # 2>>$PCP_LOG_DIR/install.log
done
