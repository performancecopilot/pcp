#!/bin/sh
#
# Simple little fake load generator - constantly wakes up and yields,
# giving the scheduler some "practice".  Not uncommon to see broken
# code do something similar to this; with a misconfigured timeout, or
# some similarly incorrect delay calculation.
#

while true
do
    sleep 0
done
