stages:
- stage: Release
  jobs:
  - job: bintray
    strategy:
      matrix:
        ${{ each distribution in parameters.distributions }}:
          ${{ distribution }}:
            distribution: ${{ distribution }}
    pool:
      vmImage: "ubuntu-latest"
    steps:
    - checkout: none
    - script: echo release to bintray
