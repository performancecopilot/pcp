ansible_ssh_common_args: "-o StrictHostKeyChecking=no"

resource_group: PCP_Builds

distributions:
  fedora30:
    name: Fedora 30
    vm_size: Standard_B2s
    base_image:
      publisher: tunnelbiz
      offer: fedora
      sku: fedora30
      version: latest
    plan:
      publisher: tunnelbiz
      product: fedora
      name: fedora30
    hostvars:
      ansible_python_interpreter: /usr/bin/python3
  ubuntu1804:
    name: Ubuntu 18.04 (Bionic)
    vm_size: Standard_B2s
    base_image:
      publisher: Canonical
      offer: UbuntuServer
      sku: 18.04-LTS
      version: latest
    hostvars:
      ansible_python_interpreter: /usr/bin/python3
