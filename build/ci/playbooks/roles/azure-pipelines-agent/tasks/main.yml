- name: Install common packages
  package:
    name:
      - git
      - ansible
    state: present
  become: yes

- import_tasks: pipelines_agent.yml

- import_tasks: pcp_build_dependencies.yml
  when: pcp_commit is defined
