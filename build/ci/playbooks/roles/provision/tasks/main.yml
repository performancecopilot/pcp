- name: Set hostvars
  set_fact:
  args: "{{ distribution_opts.hostvars }}"
  when: distribution_opts.hostvars is defined

- name: Install common packages
  package:
    name:
      - git
    state: present
  become: yes

- import_tasks: pcp_build_dependencies.yml
  when: pcp_dependencies|bool
