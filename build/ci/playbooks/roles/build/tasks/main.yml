- name: Checkout PCP source code
  git:
    repo: "https://github.com/performancecopilot/pcp.git"
    version: "{{ pcp_commit }}"
    dest: /home/pcp/pcp
    depth: 1

- name: Build PCP
  shell: set -o pipefail; ./Makepkgs --verbose |& tee /home/pcp/output.log
  args:
    chdir: /home/pcp/pcp
