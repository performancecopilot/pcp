#!/bin/sh
#

for file in src/*/*.cpp src/*/*.h
do
    if grep -q TODO $file
    then
	echo $file:
	awk <$file '
/TODO/		{ print; want = 1; next }
want == 1	{ if ($0 ~ /\/\//)
			print
		  else {
		  	want = 0
			print ""
		  }
		}' \
	| sed \
	    -e 's/^[ 	]*//'
    fi
done
